apiVersion: 1

providers:
  - name: 'EMS Dashboards'
    orgId: 1
    folder: 'Energy Management'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
